# DSA Basics

This folder contains beginner-level DSA programs:

- `MaxInArray.java` — find maximum element (O(n))
- `VowelCount.java` — count vowels in a string (O(n))
- `PalindromeCheck.java` — check palindrome (O(n))

These programs help build a strong foundation for coding interviews.
